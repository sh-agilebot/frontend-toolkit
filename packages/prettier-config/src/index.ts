import { type Options } from 'prettier';

const config: Options = {
  tabWidth: 2,
  singleQuote: true,
  trailingComma: 'none',
  bracketSpacing: true,
  bracketSameLine: true,
  useTabs: false,
  arrowParens: 'avoid',
};

export default config;
